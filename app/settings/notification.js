'use strict';

angular.module('settings.notification', [])
    .factory('SettingNotification', ['SettingServices', function(SettingServices) {
	
    }])
    .controller('SettingNotificationCtrl', ['$scope', function($scope) {
	
    }]);
